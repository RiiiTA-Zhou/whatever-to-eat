# Improvement Plan

## 1. RAG 搜索：扩充本地知识库

**问题**：当前 `data/recipes.json` 菜谱数量有限，覆盖的菜系和口味不够广，推荐容易重复。

**改进方向**：
- 从高质量菜谱网站（下厨房、豆果美食、Allrecipes 等）批量爬取菜谱数据
- 建立数据清洗 pipeline（去重、标准化格式、提取关键食材和难度）
- 支持动态扩展：通过 API 或管理后台增量添加新菜谱到 Chroma 向量库，无需重建整个索引
- 定期更新 embedding，确保新菜谱可被搜索到

## 2. Web 搜索：替换为专业菜谱源

**问题**：当前 `web_search` 使用 DuckDuckGo 通用搜索，结果质量参差不齐，且无法获取结构化菜谱信息。

**改进方向**：
- 替换为针对特定菜谱网站的结构化爬取（如 Allrecipes、Serious Eats、下厨房）
- 支持英文菜谱爬取 + 自动翻译（调用 LLM 翻译为中文），大幅扩充可用数据源
- 对爬取结果做结构化解析（菜名、食材、步骤、难度、耗时），而非返回纯文本片段
- 可考虑接入 Spoonacular 等专门食谱 API

## 3. 记忆：从文件迁移到数据库

**问题**：当前 `UserMemoryManager` 使用 JSON 文件存储（`users_history/{user_id}.json`），并发读写不安全，难以扩展，无法做复杂查询。

**改进方向**：
- 替换为关系型数据库（SQLite 起步，后续可上 PostgreSQL）
- 设计用户表、偏好表、饮食历史表，支持高效查询和事务
- 解决并发问题（多个 agent 实例同时读写同一用户）
- 为后续功能（用户画像分析、推荐冷启动）打下数据基础
- API 层增加对应的 CRUD 端点

## 4. 记忆更新：优化厨艺难度触发

**问题**：`difficulty_preference`（厨艺水平）的更新很少被触发。当前只有在用户明确说"做点简单的/难的"时才会更新。

**改进方向**：
- 在 `update_memory_as_tool` 的 trigger 逻辑中增加隐式推断：
  - 用户说"我是新手"、"没做过饭" → 设 difficulty = 1
  - 用户频繁搜索高难度菜谱 → 主动询问是否需要提升难度
  - 用户抱怨"太复杂了"、"步骤太多" → 降低难度偏好
- 在 `update_from_conversation` 的 prompt 中强化对厨艺相关表达的识别
- 在对话中 agent 主动 probe（"这道菜难度 4/5，你觉得 ok 吗？"）

## 5. 营养学知识扩展：食材功效标注

**问题**：当前 agent 在 prompt 中提及"了解营养学和饮食搭配"，但实际知识只依赖 LLM 自身能力，没有结构化的营养学知识库支撑，推荐时无法针对性地解释"为什么要吃这个"。

**改进方向**：
- 建立食材-功效知识库，标注常见食材的食用效果（如：山药→健脾养胃、红枣→补气血、苦瓜→清热降火、三文鱼→富含 Omega-3 有益心血管）
- 覆盖季节养生（如夏季祛湿、秋季润燥）、食疗搭配（如姜+红糖驱寒）
- 在 `search_recipes_with_filters` 中支持按"功效"过滤（如搜索"健脾养胃"能匹配山药排骨汤）
- 推荐时在"推荐理由"中引用功效知识，增强说服力（"这道菜中的山药有健脾养胃的作用，适合你最近说的消化不好"）
- 可考虑接入中医药膳基础知识，形成差异化特色

## 6. 输出模式：菜谱卡片化

**问题**：当前 agent 以纯文本段落输出推荐，信息密度低，用户难以快速浏览和比较多个推荐。

**改进方向**：
- 改为结构化卡片输出，包含：
  - 菜名、图片（如果有）
  - 难度星级、预计耗时
  - 关键食材一览
  - 步骤概览（折叠/展开）
  - 推荐理由（结合用户偏好）
- Gradio 前端使用 Markdown/HTML 卡片布局渲染
- API 返回结构化 JSON（`/chat_sync`），前端自由渲染
- 多推荐并列对比（如"推荐 3 道菜，选一个"）
